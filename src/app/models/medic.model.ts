export class Medic {

  constructor(
    public name?: string,
    public image?: string,
    public user?: string,
    public hospital?: string,
    // tslint:disable-next-line:variable-name
    public _id?: string
  ) {
  }
}
